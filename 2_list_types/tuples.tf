# Note that type `list` is ordered and can be referenced with [0], [1], etc..
# variable "tuple" {
#   default = [1, "2", false]
#   type    = tuple(number, string, bool)
# }


# Only one who can have different types compared to list or set
